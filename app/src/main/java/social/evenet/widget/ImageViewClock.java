package social.evenet.widget;

import android.content.Context;
import android.graphics.Canvas;
import android.util.AttributeSet;
import android.view.MotionEvent;
import android.view.View;

/**
 * Created by Alexander on 05.11.2014.
 */
public class ImageViewClock extends View {
    public ImageViewClock(Context context) {
        super(context);
    }

    public ImageViewClock(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public ImageViewClock(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
    }

    @Override
    public boolean onTouchEvent(MotionEvent event) {
        return super.onTouchEvent(event);
    }




}
